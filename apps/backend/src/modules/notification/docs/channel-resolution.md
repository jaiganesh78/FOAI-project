# Channel Resolution Document

## Channel Resolution Policy
- Candidate channels derived from priority (`CRITICAL` -> SMS, PUSH, IN_APP, EMAIL), policy precedence, and citizen preference.
- Respects `NotificationPreference` opt-outs per channel.
- Always ensures `IN_APP` is available as fallback.
